{
  "name": "ponder",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "react": "^18.3.1"
  }
}
